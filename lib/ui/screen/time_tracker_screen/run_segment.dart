import 'package:flutter/material.dart';

class RunSegment extends StatelessWidget {
  const RunSegment({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('Run')]);
  }
}
