import 'package:flutter/material.dart';

class SwimSegment extends StatelessWidget {
  const SwimSegment({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text('Swim')]);
  }
}
